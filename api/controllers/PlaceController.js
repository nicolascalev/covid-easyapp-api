/**
 * PlaceController
 *
 * @description :: Server-side actions for handling incoming requests.
 * @help        :: See https://sailsjs.com/docs/concepts/actions
 */

module.exports = {
  

  /**
   * `PlaceController.name()`
   */
  name: async function (req, res) {
    return res.json({
      todo: 'name() is not implemented yet!'
    });
  },

  /**
   * `PlaceController.quantity()`
   */
  quantity: async function (req, res) {
    return res.json({
      todo: 'quantity() is not implemented yet!'
    });
  },

  /**
   * `PlaceController.cases()`
   */
  cases: async function (req, res) {
    return res.json({
      todo: 'cases() is not implemented yet!'
    });
  }

};

