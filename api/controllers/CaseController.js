/**
 * CaseController
 *
 * @description :: Server-side actions for handling incoming requests.
 * @help        :: See https://sailsjs.com/docs/concepts/actions
 */

module.exports = {
  

  /**
   * `CaseController.name()`
   */
  name: async function (req, res) {
    return res.json({
      todo: 'name() is not implemented yet!'
    });
  },

  /**
   * `CaseController.status()`
   */
  status: async function (req, res) {
    return res.json({
      todo: 'status() is not implemented yet!'
    });
  },

  /**
   * `CaseController.places()`
   */
  places: async function (req, res) {
    return res.json({
      todo: 'places() is not implemented yet!'
    });
  }

};

